set-option -g history-limit 5000
bind r source-file ~/.tmux.conf

set -g status-bg colour248

set -g status-right "\
#(curl -s 'https://wttr.in/SLC?format=3' 2>/dev/null | iconv -c -f utf-8 -t ascii | sed 's/  / /g' || echo '...') | \
batt: #(pmset -g batt | awk '/InternalBattery/ {print $3}' | sed 's/;//') | \
%H:%M "
