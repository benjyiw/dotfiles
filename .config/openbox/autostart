# ~/.config/openbox/autostart

# disable bell
xset -b

# start pulse audio after a bit
(sleep 5s && pulseaudio --start) & 

# configure background with feh
[[ -x ~/.fehbg ]] && ~/.fehbg

compton -b &

[[ -r ~/.config/conky/conky.conf ]] && conky -d -c ~/.config/conky/conky.conf &
